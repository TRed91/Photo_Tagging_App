function StartButton(){

    const styles = {
        marginTop: "3rem"
    }

    const handleClick = () => {};

    return(
        <div style={styles}>
            <button onClick={handleClick}>Start</button>
        </div>
    )
}

export default StartButton;